#!/bin/sh
# Run from the root of the projects

# Stuff to install on your host machine to make building/debugging easier
# Assumes we're running in an Ubuntu environment

sudo apt-get install gdb-multiarch